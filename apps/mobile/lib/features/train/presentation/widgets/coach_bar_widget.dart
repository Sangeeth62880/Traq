import 'package:flutter/material.dart';

class CoachBarWidget extends StatelessWidget {
  const CoachBarWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(8.0),
      child: const Center(
        child: Text('Coach Bar Widget (Phase 3 Integration)'),
      ),
    );
  }
}
