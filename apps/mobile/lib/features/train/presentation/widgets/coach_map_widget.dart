import 'package:flutter/material.dart';

class CoachMapWidget extends StatelessWidget {
  const CoachMapWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(8.0),
      child: const Center(
        child: Text('Coach Map Widget (Phase 3 Integration)'),
      ),
    );
  }
}
